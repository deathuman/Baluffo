# 0.2.0 Release Readiness Plan

> - **Status:** Draft plan, pending release-owner review
> - **Use this when:** evaluating whether the next public Baluffo release should be promoted from the `0.1.x` train to `0.2.0`
> - **Canonical for:** proposed release scope, release-risk checklist, and draft changelog summary before metadata is changed
> - **Not canonical for:** current shipped version, release metadata, manifest schema versions, data schema versions, or published release notes
> - **Then inspect:** [`../RELEASE.md`](../RELEASE.md), [`../testing.md`](../testing.md), [`../storage-contract.md`](../storage-contract.md), [`../sync-contract.md`](../sync-contract.md), and [`../CHANGELOG.md`](../CHANGELOG.md)
> - **Last updated:** 2026-05-13

## Summary

Baluffo has accumulated a release-sized set of changes since `v0.1.33`.
The local commit review found `565` commits between `v0.1.33` and `HEAD`
on 2026-05-13, with work concentrated in runtime storage, source-sync,
Admin/Ops observability, source-policy and dedup tooling, packaged desktop
hardening, performance tooling, and repo safety guardrails.

This plan evaluates `0.2.0` as the next public release target. It is a draft
planning document only. It does not change the app version, changelog, tags,
release assets, manifests, updater metadata, or data schema metadata.

## Release Target

- Target public version under review: `0.2.0`.
- Current latest local release tag observed: `v0.1.33`.
- Current app version source observed: `APP_VERSION = "0.1.33"` in `src/app_version.py`.
- Recommended tag if approved: `v0.2.0`.
- Treat this as a larger public release-line bump, not as an intentionally breaking data-contract release unless migration review proves otherwise.

## Main Release Themes

### Runtime Storage Authority

- Added SQLite/WAL runtime storage for task lifecycle, sync runs, source runs,
  jobs feed exports, and source registry rows.
- Added storage health diagnostics and packaged storage validation.
- Preserved compatibility exports so existing frontend and release flows can keep
  reading stable JSON projections where required.

### Source Sync v3

- Added content-addressed gzip shard bundles and schema-v3 source-sync manifests.
- Added sharded reads and writes, changed-shard push detection, bounded shard
  garbage collection, and per-shard push progress.
- Tightened source-sync snapshot limits, validation, idempotency, checkpoint
  tagging, and packaged sync-config verification.

### Admin/Ops Lifecycle Visibility

- Added richer current-run, completed-run, task lifecycle, progress, diagnostics,
  source-policy, and dedup visibility in Admin/Ops.
- Hardened task liveness, fetch completion authority, live task polling, and
  progress projection behavior.
- Added diagnostic copy and evidence-oriented review surfaces without making
  diagnostics the lifecycle authority.

### Source Policy, Registry, and Dedup Tooling

- Added source-policy soak reporting, provider migration advisory evidence,
  manual migration-link review, provider/static overlap checks, and safer static
  suppression proposal flows.
- Added dedup evidence, review queues, monitor-vs-blocking classification,
  Google Sheets role-bucket diagnostics, provider/static disagreement review,
  and lifecycle-readiness gates.
- Corrected registry conflict, provider/static, static-source, and dedup pressure
  handling so monitor-only debt remains visible without blocking lifecycle readiness.

### Packaged Desktop and Release Hardening

- Added packaged storage, jobs-feed parity, fetch evidence, sync rehearsal,
  update rehearsal, orphan-reclaim, browser-job, and jobs-pipeline coverage.
- Included SQLite runtime support and storage modules in packaged and ship-bundle
  paths.
- Hardened desktop startup/session ownership, packaged smoke readiness, runtime
  resource bundling, and release-critical validation paths.

### Performance, CI, and Repo Safety

- Added startup, frontend, bridge, sync, discovery, and fetch benchmark/profiling
  signals.
- Added benchmark workflows, performance baselines, trend reporting, and startup
  regression gates.
- Added gitleaks secret scanning, Python dependency security audit wiring,
  bridge route inventory guardrails, repo-health policies, and broader typed/lint
  guardrails.

### Documentation and Maintainability

- Reorganized active docs around the docs index, release guide, storage/sync
  contracts, source-policy runbook, testing guide, and AI/tooling guardrails.
- Archived completed planning records and kept active plans focused on current
  follow-up work.
- Split many large compatibility roots and runtime files into narrower leaf
  modules while preserving public facades where needed.

## Draft Changelog Section

This section is a proposal only. Do not move it into `docs/CHANGELOG.md` until
the `0.2.0` release scope, release date, migration sign-off, and validation
results are confirmed.

```markdown
## [0.2.0] - 2026-05-13

### Added
- Runtime SQLite/WAL storage authority for task lifecycle, sync runs, source runs, jobs feed exports, and source registry rows, with storage-health diagnostics and packaged storage validation.
- Source-sync schema v3 support with content-addressed gzip shards, manifest-based reads/writes, changed-shard push detection, bounded shard garbage collection, and per-shard push progress.
- Source-policy, provider/static, registry-conflict, and dedup review tooling, including soak reports, provider migration advisory evidence, manual migration-link review, safer static suppression proposals, and monitor-vs-blocking dedup gates.
- Admin/Ops task lifecycle visibility, current-run cards, run detail views, diagnostics copy, source-policy review panels, and live task progress surfaces.
- Performance and release-safety tooling, including startup/perf probes, benchmark reports, packaged storage/fetch evidence smoke coverage, gitleaks pre-commit scanning, dependency audit wiring, and bridge route inventory guardrails.

### Changed
- Jobs feed, source-run details, task lifecycle state, and source registry reads now move through the new runtime storage authority while preserving compatibility exports for existing frontend and release flows.
- Source discovery and jobs fetching internals were heavily simplified into narrower helpers, shared runners, and reusable audit/recovery flows without changing the public app model.
- Packaged desktop and ship-bundle release paths now include stronger storage, sync, browser-job, orphan-reclaim, jobs-pipeline, and updater rehearsal coverage.
- Runtime artifacts and source registry defaults are now split more clearly between checked-in seeded defaults and local runtime state.
- Documentation was reorganized around the active docs index, release guide, storage/sync contracts, source-policy runbook, and AI/tooling guardrails.

### Fixed
- Pipeline lifecycle, fetch completion authority, Admin progress restoration, packaged smoke readiness, and startup/session ownership were hardened across desktop and browser-local flows.
- Registry conflict, provider/static disagreement, Google Sheets role-bucket, static source conflict, and dedup review pressure handling were corrected so monitor-only debt no longer blocks lifecycle readiness.
- Source-sync snapshot limits, retry/idempotency behavior, checkpoint tagging, source-health parity, and packaged sync config validation were tightened.
- Windows file-lock and registry journal maintenance paths now retry more safely and keep runtime evidence handling bounded.
- CI, lint, mypy, frontend smoke, packaged desktop, and release-preflight regressions from the broad refactor work were resolved.

### Security
- Added gitleaks secret scanning guardrails and Python dependency security audit coverage to the repo/release safety path.
```

## Release Risk Checklist

Before approving `0.2.0` as the next release:

1. Confirm whether the runtime storage and source-sync changes require a
   `data_schema_version` bump, or explicitly sign off that no release data
   migration is required.
2. Define the migration statement for release notes and manifests:
   `no migration required`, or a concrete apply/verify/rollback migration plan.
3. Confirm rollback policy, including whether `rollback_allowed` should remain
   false for the desktop update manifest or be enabled only after a validated
   rollback rehearsal.
4. Rehearse update from `0.1.33` to `0.2.0` on staging with persisted local data.
5. Confirm storage health, startup readiness, source-sync behavior, and support
   bundle generation after the staged update.
6. Confirm GitHub release secrets and desktop update signing inputs are present
   in the release workflow environment.
7. Confirm release notes can be extracted from the final `docs/CHANGELOG.md`
   `0.2.0` section once it is approved.
8. Keep the previous release available for rollback and incident response.

## Non-Goals For This Draft

- Do not update `src/app_version.py`.
- Do not update `docs/CHANGELOG.md`.
- Do not create, move, delete, or recreate tags.
- Do not build or publish release artifacts.
- Do not change update manifests, release workflow behavior, updater versions,
  manifest schema versions, or data schema versions.
- Do not add new runtime, Python, or Node dependencies.

## Validation Plan For A Future Release Pass

When this draft is approved and release prep begins:

1. Update the app version and changelog on the release commit.
2. Run the canonical release preflight:

   ```powershell
   npm run release:preflight
   ```

3. Build exact-version artifacts:

   ```powershell
   npm run build:ship-bundle -- --bundle-version 0.2.0
   npm run build:portable-exe -- --bundle-version 0.2.0
   ```

4. Rehearse packaged desktop update from `0.1.33` to `0.2.0`.
5. Validate release-note extraction from `docs/CHANGELOG.md`.
6. Publish only after staged startup, update, storage health, local-data
   preservation, source-sync, and rollback expectations are signed off.

## Assumptions

- `0.2.0` is intended as a larger public release-line bump after the post-`0.1.33`
  improvement wave.
- The release is not intended to break current saved-job, bridge, storage export,
  or source-sync compatibility surfaces.
- Active advisory/future plans remain deferred unless explicitly added to the
  `0.2.0` release scope.
- This document is intentionally editable and remains non-canonical until release
  owner review promotes its decisions into `docs/CHANGELOG.md`, version metadata,
  and release artifacts.
